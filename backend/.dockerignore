# Keep the build context (and image) small and reproducible.
.venv/
**/__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
tests/
*.md
# Secrets never belong in the image — config arrives via env_file/compose.
.env
.env.*
